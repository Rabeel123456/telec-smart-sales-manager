import {useState} from 'react'
import {supabase} from '../lib/supabase'
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);setError('');const{error}=await supabase.auth.signInWithPassword({email,password});setLoading(false);if(error)setError(error.message)}return <div className='login-page'><form className='login-card' onSubmit={submit}><div className='login-logo'>T</div><h1>TELEC Smart Sales Manager</h1><p>Sign in to continue</p><label>Email<input type='email' value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type='password' value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className='error'>{error}</div>}<button className='primary' disabled={loading}>{loading?'Signing in...':'Sign In'}</button></form></div>}
